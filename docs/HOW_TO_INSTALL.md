# 📱 안드로이드 앱 설치 및 실행 방법

현재 개발된 환율 계산기는 **웹 애플리케이션(Web App)**이므로, 스마트폰 브라우저(Chrome)를 통해 접속한 뒤 **"홈 화면에 추가"** 기능을 사용하면 앱처럼 설치하여 사용할 수 있습니다.

## 1. 준비 사항 (완료됨)
이미 프로젝트 폴더에 다음 파일들을 추가해두었습니다.
- `manifest.json`: 앱 설치 정보 파일
- `icon.png`: 앱 아이콘 (512x512)
- `index.html`: 설치 정보 연결 완료

---

## 2. 스마트폰에서 실행하는 방법

스마트폰에서 이 파일을 실행하려면 **웹 서버**가 필요합니다. 다음 두 가지 방법 중 하나를 선택하세요.

### [방법 A] GitHub Pages로 배포하기 (가장 추천 ⭐)
이 방법은 인터넷만 되면 어디서든 접속할 수 있어 가장 편리합니다.

1. **GitHub 저장소 생성**: GitHub에 새 저장소(repository)를 만듭니다.
   > **⚠️ 중요**: 무료 계정에서 GitHub Pages를 쓰려면 저장소를 반드시 **Public (공개)**으로 설정해야 합니다. Private으로 만들면 Pages 기능이 활성화되지 않습니다.
2. **파일 업로드**: 현재 폴더(`obsidian-feynman`)의 모든 파일(`index.html`, `styles.css`, `app.js`, `manifest.json`, `icon.png`)을 업로드합니다.
3. **Pages 설정**:
   - 저장소의 [Settings] > [Pages] 메뉴로 이동합니다.
   - Branch를 `main` 또는 `master`로 선택하고 [Save]를 누릅니다.
4. **링크 접속**: 잠시 후 생성되는 주소(예: `https://username.github.io/repo-name`)를 복사합니다.
5. **설치**:
   - 안드로이드 폰에서 Chrome 브라우저로 위 주소에 접속합니다.
   - 화면 하단 또는 메뉴(⋮)에서 **"홈 화면에 추가"** 또는 **"앱 설치"**를 누릅니다.
   - 이제 바탕화면에 앱 아이콘이 생기며, 인터넷 없이도(설정 시) 실행 가능한 앱처럼 작동합니다.

### [방법 B] 같은 와이파이(Wi-Fi)에서 실행하기 (임시 테스트용)
PC와 스마트폰이 같은 와이파이에 연결되어 있어야 합니다.

1. **VS Code에서 실행**:
   - VS Code 확장 프로그램 **"Live Server"**를 설치하고, `index.html`을 우클릭하여 "Open with Live Server"를 실행합니다.
   - 또는 터미널에서 `npx http-server` 명령어를 입력합니다.
2. **IP 주소 확인**:
   - 터미널(CMD)에서 `ipconfig`를 입력하여 PC의 **IPv4 주소**를 확인합니다 (예: `192.168.0.10`).
3. **폰에서 접속**:
   - 폰 브라우저 주소창에 `http://[PC IP주소]:5500` (또는 해당 포트 번호)를 입력합니다.
   - 접속 후 메뉴(⋮)에서 **"홈 화면에 추가"**를 눌러 설치합니다.

---

### 💡 설치 후 팁
- 앱으로 설치하면 브라우저 주소창 없이 **전체 화면**으로 쾌적하게 사용할 수 있습니다.
- 오프라인 모드 데이터가 내장되어 있어, 인터넷이 끊겨도 기본적인 계산은 가능합니다.
